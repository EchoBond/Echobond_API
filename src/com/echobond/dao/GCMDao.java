package com.echobond.dao;

public class GCMDao {
	/*
	updateRegId();
	//checking reg id
	String userId = rp.getRs().getString("id");
	String regId = request.getParameter("regId");
	rp.close();
	rp = DBUtil.getInstance().query(sqlProperties.getProperty("loadRegIdByUserId"), new Object[]{userId});
	if(!rp.getRs().next())
		DBUtil.getInstance().update(sqlProperties.getProperty("updateRegId"), new Object[]{userId, regId, DateUtil.dateToString(new Date())});
	//end of reg id checking
	 */
	
}
